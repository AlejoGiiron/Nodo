{
  "buildCommand": "pnpm build && rm -f dist/assets/*.map",
  "routes": [
    { "src": "/.*\\.map$", "status": 404 },
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
