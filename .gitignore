# Entornos
.env
.env.local
.env.*.local
.env.test
!.env.example
!.env.test.example
# staging: la plantilla se versiona, el archivo con secretos NO
docs/staging/.env.staging

# Node
node_modules/
.pnpm-store/

# Build outputs
dist/
*.tsbuildinfo
.next/
out/
build/
.expo/

# Supabase local
supabase/.branches/
supabase/.temp/

# Claude Code
.claude/settings.local.json
# instrumento de medicion local de la deuda 22 (disparos del hook), no se versiona
.claude/hook-ledger.jsonl
.claude/hook-ledger.stats.json

# Editores
.vscode/settings.json
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Testing
coverage/
test-results/
playwright-report/
playwright/.cache/

# Claude Design handoff — vive en Claude Design, no en el repo
design_handoff_*/

# Misc
*.pem
.vercel

# Temporal que genera vite al cargar vite.config.ts
vite.config.ts.timestamp-*.mjs

# ── Datos del cliente y planes de sesión: NO viajan al repo ──────────────
# `Control Mp.xlsx` nombra a 11 clientes reales de Muscle Pro (PII de terceros).
# Una vez en el historial de GitHub no se saca sin reescribir todos los commits.
docs/Control Mp.xlsx
docs/PLAN-2026-09-02.md
